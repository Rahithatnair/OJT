
# Write a Python program to calculate the square of a number using a lambda function.

square = lambda x: x ** 2

number = float(input("Enter a number: "))
print("The square of", number, "is:", square(number))
